apiVersion: admissionregistration.k8s.io/v1beta1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: "params-max-replicas-policy-binding"
spec:
  policyName: "params-max-replicas-policy"
  paramRef:
    name: rule-config
    namespace: demo
  validationActions: [Deny]
  matchResources:
    namespaceSelector:
      matchLabels:
        environment: test